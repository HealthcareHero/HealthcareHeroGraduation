import css from './index.module.css'
import { Css } from './index.type'

export const styles: Css = {
  container: css.container,
  actionSection: css["action-section"],
  author: css.author,
  meta: css.meta,
  message: css.message,
  buttons: css.buttons,
  divider: css.divider
}
